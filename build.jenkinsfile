pipeline {
  agent any
  stages{
    stage('Checkout from Git') {
            checkout scm:
            [$class: 'GitSCM', branches: [[name: 'main']],
             extensions: [[$class: 'CloneOption', noTags: false]]]
            
        }
  }
}

       
